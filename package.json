{
  "name": "m-",
  "description": "Mdash is a design system based on web standards. It's extremely small (see comparison chart), fast, simple, and compatible with any JavaScript framework. \n",
  "version": "2.0.1",
  "homepage": "https://m-docs.org",
  "repository": {
    "type": "git",
    "url": "https://github.com/jfbrennan/m-"
  },
  "bugs": "https://github.com/jfbrennan/m-/issues",
  "files": [
    "/dist"
  ],
  "license": "MIT",
  "contributors": [
    {
      "name": "Jordan Brennan",
      "url": "https://github.com/jfbrennan"
    }
  ],
  "keywords": [
    "ui",
    "web",
    "css",
    "html",
    "frontend",
    "webcomponents",
    "customelements"
  ],
  "browserslist": [
    "last 1 version",
    "> 5%",
    "not dead"
  ],
  "scripts": {
    "watch": "nodemon --watch src --ext css,js --exec npm run build",
    "build": "rm -r dist; mkdir dist && npm run css && npm run css:custom-properties && npm run js && npm run icons && cp -R dist/** docs/public",
    "css": "postcss ./src/m-all.css | lightningcss --minify -o ./dist/m-min.css /dev/stdin",
    "css:custom-properties": "cp -R src/custom-properties.css dist",
    "icons": "cp -R ./src/Simple-Line-Icons.woff2 dist",
    "js": "terser ./src/*.js --mangle -o ./dist/m-min.js"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.8",
    "gulp": "^4.0.2",
    "gulp-replace": "^1.1.4",
    "lightningcss-cli": "^1.17.1",
    "postcss": "^8.4.20",
    "postcss-cli": "^10.1.0",
    "postcss-custom-properties": "^12.1.11",
    "postcss-import": "^12.0.1",
    "terser": "^5.16.1"
  },
  "dependencies": {}
}
