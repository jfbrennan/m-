<h1 class="mar-b-xs">Details</h1>
<h2 class="txt-gray-5 mar-t-none">Expandable container used for disclosing additional content</h2>
<h2 id="demo">Demo</h2>
<code-sample>
<template>
  <m-details>
    <summary>Summary</summary>
    <p>Details about this thing</p>
  </m-details>
</template>
</code-sample>
<p>See more <a href="/examples#Details">examples</a></p>
<h2 id="api">API</h2>
<h3 id="tag">Tags</h3>
<table layout="fixed">
  <thead>
  <tr>
    <th colspan="2">Name</th>
    <th colspan="2">Type</th>
    <th colspan="6">Content</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td colspan="2"><code>m-details</code></td>
    <td colspan="2">Custom Element</td>
    <td colspan="6"><code>&lt;summary&gt;</code> as first child and any other content</td>
  </tr>
  <tr>
    <td colspan="2"><code>summary</code></td>
    <td colspan="2">Native element</td>
    <td colspan="6">Any, but should be a “summary, caption, or legend for the rest of the contents"</td>
  </tr>
  </tbody>
</table>
<h3 id="attributes">Attributes</h3>
<table layout="fixed">
  <thead>
  <tr>
    <th colspan="2">Name</th>
    <th colspan="2">Value</th>
    <th colspan="6">Description</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td colspan="2"><code>open</code></td>
    <td colspan="2">Boolean attribute</td>
    <td colspan="6">When present the details are shown (everything after summary). When removed details are hidden.</td>
  </tr>
  </tbody>
</table>
<h3 id="events">Events</h3>
<table layout="fixed">
  <thead>
  <tr>
    <th colspan="2">Name</th>
    <th colspan="2">Detail</th>
    <th colspan="6">Description</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td colspan="2"><code>toggle</code></td>
    <td colspan="2">None</td>
    <td colspan="6">Fires after Details was opened or closed.</td>
  </tr>
  </tbody>
</table>
<h2 id="guidelines">Guidelines</h2>
<h3 id="title">What about &lt;details&gt;?</h3>
<p>This element attempts to match the <a href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLDetailsElement" target="_blank" rel="noopener"><code>HTMLDetailsElement</code></a> interface as close as possible so when browser support for <code>&lt;details&gt;</code> is sufficient it will be removed from M- in favor of the native element. Breaking changes should be limited to just the "m-" prefix.</p>
<h3 id="necessary">Ask yourself if the content is necessary</h3>
<p>Although Details and <a href="/accordion">Accordion</a> are great tools for progressive disclosure, you should always consider if the hidden content is necessary or if it could just be removed altogether.</p>
<h3>Clickable elements inside summary</h3>
<p>Sometimes <code>&lt;summary&gt;</code> will need to include a button or other interactive element that should not toggle Details. Your event handler needs to use <code>stopPropogation</code>. Here's a rough example:</p>
<code-sample>
  <script>
    // Clicking "Clear" will not open/close Details
    const clearHistory = e => e.stopPropagation();
  </script>

  <template>
    <m-details>
      <summary>
        <span is="a">Show report history</span>
        <span  onclick="clearHistory(event)" is="a" class="mar-l-md">Clear</span>
      </summary>
      <p>Long list of report history...</p>
    </m-details>
  </template>
</code-sample>
<h3 id="accessibility">Accessibility</h3>
<p>aria-exanded=true.</p>
